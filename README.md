# سامانه هوشمند معاملات طلای آبشده (زرین) 🪙

این پروژه یک پلتفرم جامع و مدرن برای خرید و فروش آنلاین طلای آبشده است که با تمرکز بر امنیت، سرعت و تجربه کاربری عالی (UI/UX) طراحی شده است.

## 🚀 قابلیت‌های کلیدی

### ۱. پنل کاربری (User Panel)
*   **احراز هویت:** ثبت‌نام کاربر و سیستم تایید هویت توسط ادمین.
*   **مدیریت دارایی:** نمایش لحظه‌ای موجودی ریالی و طلایی به همراه وضعیت بدهکاری/بستانکاری.
*   **تابلوی قیمت زنده:** بروزرسانی خودکار قیمت خرید، فروش و گرم ۱۸ عیار.
*   **فریز قیمت:** قابلیت تثبیت قیمت به مدت ۱۰ ثانیه جهت ثبت معامله با نرخ قطعی.
*   **معاملات آنی:** ثبت سفارشات خرید و فروش با تاثیر لحظه‌ای بر موجودی حساب.
*   **تاریخچه تراکنش‌ها:** مشاهده لیست کامل معاملات با قابلیت فیلتر و جزئیات دقیق.
*   **ثبت حواله:** امکان ثبت حواله‌های بانکی و بارگذاری رسید جهت تایید توسط مدیریت.

### ۲. پنل مدیریت (Admin Panel)
*   **مدیریت کاربران:** تایید یا رد درخواست‌های ثبت‌نام و مدیریت دسترسی‌ها.
*   **کنترل بازار:** تنظیم دستی حباب خرید و فروش و قیمت‌های پایه.
*   **توقف معاملات:** کلید اضطراری برای قطع تمامی معاملات در زمان نوسانات شدید بازار.
*   **مانیتورینگ:** مشاهده تراکنش‌های سیستمی و تایید حواله‌های ریالی.

### ۳. بخش تحلیل و نمودار
*   **نمودار پیشرفته:** نمایش تغییرات قیمت در بازه‌های زمانی مختلف با استفاده از Recharts.
*   **تحلیل هوشمند (AI):** بهره‌گیری از مدل **Gemini 3 Flash** برای ارائه تحلیل‌های کوتاه و کاربردی از وضعیت بازار به صورت خودکار.

## 🛠 تکنولوژی‌های مورد استفاده
*   **React:** برای ساختاردهی کامپوننت‌محور و بهینه.
*   **Tailwind CSS:** برای طراحی رابط کاربری مدرن و کاملاً واکنش‌گرا (Responsive).
*   **Google Gemini API:** جهت هوشمندسازی پلتفرم و تحلیل داده‌ها.
*   **Lucide React:** مجموعه آیکون‌های زیبا و سبک.
*   **Recharts:** برای نمایش بصری داده‌های بازار.
*   **Vazirmatn:** استفاده از فونت استاندارد و زیبای فارسی.

## 📦 ساختار پروژه
```text
.
├── components/
│   ├── Dashboard.tsx    # پیشخوان اصلی کاربر و بخش معاملات
│   └── AdminPanel.tsx   # مدیریت سیستم و تنظیمات قیمت
├── services/
│   └── geminiService.ts # ارتباط با هوش مصنوعی گوگل
├── constants.ts         # مقادیر ثابت و داده‌های نمونه
├── types.ts             # تعاریف تایپ‌های TypeScript
├── App.tsx              # مدیریت مسیرها (Routing) و ساختار اصلی
├── index.html           # نقطه ورود HTML با تنظیمات RTL
└── metadata.json        # تنظیمات و دسترسی‌های اپلیکیشن
```

## ⚙️ راه‌اندازی
برای اجرای پروژه، اطمینان حاصل کنید که کلید API معتبر برای Gemini در متغیرهای محیطی با نام `API_KEY` تنظیم شده باشد.

1.  فایل‌ها را در محیط توسعه خود قرار دهید.
2.  از یک سرویس‌دهنده مانند `esm.sh` برای مدیریت وابستگی‌ها استفاده شده است، لذا نیاز به نصب سنگین کتابخانه‌ها نیست.
3.  اپلیکیشن به صورت خودکار با قابلیت RTL (راست‌چین) اجرا می‌شود.

---
**توسعه داده شده توسط تیم مهندسی زرین**
*هدف ما ایجاد بستری امن و شفاف برای سرمایه‌گذاری در بازار طلا است.*
